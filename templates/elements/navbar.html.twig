<nav class="navbar navbar-expand-md navbar-dark bg-dark">
                <a class="navbar-brand" href="{{ path('home') }}">
                    <img src="{{ asset('images/logo.png') }}" width="30" height="30" class="d-inline-block align-top" alt="">
                    Sortir.com
                </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample04" aria-controls="navbarsExample04" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExample04">
        <ul class="navbar-nav mr-auto mr-auto mt-2 mt-lg-0">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('villes') }}">Ville</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('campus') }}">Campus</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="#">Evenement</a>
                </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav my-2 my-lg-0">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {% if app.user.photo is not null %}
                            <img class="img-profile rounded-circle" src="{{ asset('files/photo/' ~ app.user.photo) }}">
                            <i>{{ app.user.username }}</i>
                        {% else %}
                            <i class="fas fa-user-circle"></i> {{ app.user.username }}
                        {% endif %}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                        <a class="dropdown-item" href="{{ path('profil') }}">Profil</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item text-danger" href="{{ path('security_logout') }}">Se déconnecter</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('security_login') }}"><i class="fas fa-user"></i> Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>